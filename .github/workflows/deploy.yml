name: Deploy Isma

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/gramajo/sites/IsmaWeb
            git pull
            docker compose -f /home/gramajo/compose.yaml build isma
            docker compose -f /home/gramajo/compose.yaml up -d isma